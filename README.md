# Invoice_IQ
AI-powered invoice processing system that extracts, validates, and analyzes invoices using LLMs (Groq + LLaMA 3), with a full pipeline including parsing, validation, duplicate detection, API integration, and a modern Streamlit dashboard.
